// import { fromJS } from 'immutable';
// import { selectLogoutFormDomain } from '../selectors';

describe('selectLogoutFormDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(false).toEqual(false);
    });
});
